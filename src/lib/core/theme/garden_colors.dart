import 'package:flutter/material.dart';

class GardenColors {
  GardenColors._();

  // Backgrounds
  static const Color cream = Color(0xFFF7F5F0);       // fondo suave cálido
  static const Color parchment = Color(0xFFF0EBE1);   // fondo alterno

  // Verdes
  static const Color sageLight = Color(0xFFEBF2E8);   // chips / avatares
  static const Color sage = Color(0xFF9AB898);         // bordes suaves
  static const Color forest = Color(0xFF3D5E36);       // acción principal
  static const Color moss = Color(0xFF5F7E52);         // secundario

  // Neutros
  static const Color earth = Color(0xFF7C6B4E);
  static const Color dust = Color(0xFF8E8278);         // texto secundario (más contraste)
  static const Color dustLight = Color(0xFFE5E2DC);   // bordes de cards
  static const Color charcoal = Color(0xFF252525);    // texto principal

  // Semánticos
  static const Color errorRose = Color(0xFFDC3545);
  static const Color okGreen = Color(0xFF1DB954);
  static const Color golden = Color(0xFFE9A800);
  static const Color waterBlue = Color(0xFF2563EB);
  static const Color navBackground = Color(0xFFFFFFFF);
}
