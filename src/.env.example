# Copia este archivo como .env y rellena los valores reales.
# .env está en .gitignore — nunca lo subas al repo.

# Supabase
SUPABASE_URL=https://tslrtebdziilekddalcr.supabase.co
SUPABASE_ANON_KEY=

# Google OAuth (Firebase console → credenciales → OAuth 2.0 → client ID web/server)
GOOGLE_CLIENT_ID=
